<ul>
  <% @tasks.each do |task| %>
    <li input type=none>  <input type="checkbox" <%= task.completed ? "checked" : "" %> > <%= link_to task.title, task_path(task) %> </li>

  <% end %>
</ul>
<%= link_to "Add new task", tasks_new_path %>
